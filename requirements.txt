numpy
rasterio
matplotlib
scikit-learn          # one-class SVM + validation metrics
geopandas             # validate.py — read/reproject ground-truth shapefile

# Only needed if you enable USE_TEXTURE / USE_EDGE in config.py (weak at 10 m):
opencv-python
scikit-image
